import BrandedSpinner from '@covid/components/Spinner';
import { storiesOf } from '@storybook/react-native';
import React from 'react';

storiesOf('BrandedSpinner', module).add('default view', () => <BrandedSpinner />);
